# SELF_CHECK — обязательная проверка перед сдачей

Для каждого пункта укажи `PASS`, `FAIL` или `N/A` и доказательство `файл:строка`.
`FAIL` → исправь → повтори проверку. Без полного PASS работа не сдана.

`N/A` допустим ТОЛЬКО в условных секциях (Q, I) и требует одной строки пояснения,
почему функции нет в задаче. `N/A` в обязательных секциях = `FAIL`.

Единственное исключение — секция B при отсутствии `playwright`: инструмента нет,
проверить нечем, и это не вина работы. Тогда B1–B6 = `N/A` с причиной «нет
playwright». Строки с ID всё равно обязаны быть в отчёте: их отсутствие роняет Z6.

Тот же порядок, если запуск скрипта запретила среда («blocked for safety»,
«classifier unavailable», отказ в разрешении): пункт = `N/A`, в причине —
формулировка отказа ДОСЛОВНО, плюс строка пользователю о том, что осталось
непроверенным (`BLOCKED.md`). Без дословной цитаты
отказа это не `N/A`, а `FAIL`: «не смог запустить» без доказательства
неотличимо от «не запускал».

Номера в скобках — ссылки на RETRO в `PATTERNS.md` (1–26) и `TABLES.md` (27–40).
Пункты `(auto)` закрываются выводом `validate.py` — копируй его вердикт, не проверяй
глазами. Остальные — только твоя ответственность.

**Что проходить:**

| Секция | Где лежит | Когда |
|---|---|---|
| A, B, P, M, S, C, D, F, Z | здесь | всегда |
| Q1–Q8. SQL | `SQL.md`, раздел 5 | только если правил `<name>.data.sql` |
| I1–I16. Интерактив | `TABLES.md`, раздел 6 | только если читал `TABLES.md` |

В РЕЖИМЕ B (точечная правка) проходи A, Z, затронутые пункты и — обязательно —
секцию «Связка» из `REGRESSION.md`.

## A. Машинная проверка (делается ПЕРВОЙ)

- [ ] A1. Запущен `python3 <скилл>/check.py <путь>.chart.js` (для болванки —
      `validate.py` с `--template`), оба вывода вставлены в ответ дословно.
- [ ] A2. В выводе `0 FAIL`. Любой FAIL → исправить и перезапустить.
- [ ] A3. Каждый WARN либо устранён, либо объяснён одной строкой.
- [ ] A4. Строк `ОСПОР` нет — либо каждая выписана в NOTES §6 и названа
      пользователю словами. `ОСПОР` это НЕ `PASS`: проверка осталась
      непройденной, ты лишь объяснил, почему считаешь её неправой
      (`SKILL.md`, «ОСПАРИВАНИЕ ПРОВЕРКИ»).
- [ ] A5. `check.py` не печатал «ХОЖДЕНИЕ ПО КРУГУ». Напечатал — сдача
      останавливается: тот же код уже чинили, и он упал снова. Разберись,
      что сломала предыдущая правка, либо оспорь проверку (RETRO 66).
- [ ] A6. `K3` = PASS: `esc/num/toDate` совпадают с `templates/`. FAIL здесь
      означает, что БЛОК 2 написан заново, а не заполнен, — и что остальные
      FAIL, скорее всего, его следствия. Чинить их по одному бессмысленно:
      верни тела из шаблона и перепрогони (RETRO 63).

## B. Поведение в браузере (делается ВТОРОЙ)

`validate.py` смотрит на форму кода и не видит экрана. Эти пункты закрываются
ТОЛЬКО выводом `smoke.mjs` — не рассуждением о коде и не «по логике должно
работать». Зелёная секция A при красной B означает, что график не работает.

- [ ] B1. Запущен `node <скилл>/smoke.mjs <путь>.chart.js`, вывод вставлен в ответ
      дословно. `<скилл>` — папка скилла: скрипты лежат ТАМ и в проект
      не копируются, поэтому «нет файла рядом с чартом» означает неверный
      вызов, а не `N/A` (RETRO 58, ловит `validate.py` Z6b).
      Код возврата 2 (нет playwright) — это НЕ провал: B1–B6 = `N/A`
      с причиной, сдача продолжается, пользователю сказано, что экрана никто
      не видел. Установка предлагалась ещё на ШАГЕ 0 (`--env`, RETRO 50);
      отказ не переспрашивается. Ставить пакет без явного «да» нельзя.
- [ ] B2. В выводе `0 FAIL`: overlay смонтирован, canvas скрыт, корень отрисован
      и тянется за хостом на двух ширинах (`E13`), содержимое SVG умещается
      в `viewBox` (`E14`), ошибок в консоли нет.
- [ ] B3. Тултип виден при наведении на каждый триггер и гаснет после ухода
      курсора (`ET1`–`ET4`), включая проверку пикселями (`E6`) (RETRO 44).
      `N/A` здесь допустим ТОЛЬКО по причине из среды (нет playwright, запуск
      заблокирован). «Триггеры есть, но smoke их не детектирует» причиной
      НЕ является: это своё имя атрибута вместо `data-tip`/`data-kind`/
      `data-action`, проверка не пройдена, а пропущена — чини имена (RETRO 56).
      Тот же класс — имя, склеенное с префиксом (`CFG.ns + '-data-tip'`):
      в разметке видно `data-tip`, в DOM приезжает `pvt-data-tip` (RETRO 59,
      ловит `validate.py` T7b).
- [ ] B4. Повторный запуск скрипта не плодит overlay и узлы тултипа (`E8`, `E9`);
      пустой `data` даёт `CFG.text.noData` (`E11`). Клик перерисовывает
      разметку (`E7`) — либо это доказано на переключалках (`E15`), и тогда
      `E7` законно `N/A`.
- [ ] B4b. ПЕРЕЗАПУСК СКРИПТА пережили и выбор, и интерактив: выбранная вкладка
      осталась выбранной (`E18`), клики после перезапуска всё ещё меняют
      экран (`E19`). Proteus перезапускает скрипт на каждой перерисовке, и
      виджет, проходящий всё остальное, становится после неё картинкой —
      без единой ошибки в консоли (RETRO 65). `N/A` здесь означает, что
      переключалок нет вовсе.
- [ ] B5. Данные для прогона названы явно: `<name>.mock.json` (тогда цифры реальные)
      либо автомок из `CFG.fields` (тогда проверена живость, а не значения).
      Если в шапке есть «вслепую: N колонок вне `CFG.order`» — жёлтые строки
      про тултипы и SVG сначала объясняются моком, а не виджетом: заполни
      `CFG.order` и перепрогони, прежде чем что-то чинить (RETRO 66).
- [ ] B6. Вкладки, табы и переключатели реально МЕНЯЮТ ЭКРАН (`E15`), в каждый
      момент видима ровно одна панель (`E16`), смена значения в `<select>` меняет
      экран (`E17`). `E7` этого НЕ доказывает: ему хватает сменившегося класса
      `active` на кнопке, экран при этом может остаться прежним (RETRO 57).
      `E15` = `N/A` означает, что переключалок не нашлось: если они в макете
      ЕСТЬ — пометь их `role="tab"`/`aria-selected`/`data-view` или классом
      выбора, иначе они не проверены.

## P. Процесс

- [ ] P1. Первым действием режим назван вслух СЛОВАМИ («РЕЖИМ A — новая
      визуализация» / «РЕЖИМ B — точечная правка»), и отдельно объявлен уровень
      проверки по `node <скилл>/smoke.mjs --env`: полная браузерная или только
      статическая (RETRO 50).
- [ ] P2. В РЕЖИМЕ A до кода был показан и подтверждён нумерованный план.
- [ ] P3. `<name>.NOTES.md` создан ДО чтения макета и заполнялся по ходу, а не задним
      числом.
- [ ] P4. HTML покрыт диапазонами по ≤300 строк; NOTES §2 содержит все диапазоны.
      Диапазоны-образцы помечены как «по образцу N×, прочитано 2 + последняя»,
      и помечены только ОДНОРОДНЫЕ повторы из вывода `scan-layout.py`.
- [ ] P5. NOTES §1 содержит реестр всех блоков, контролов, состояний и mock-значений.
- [ ] P6. `.chart.js` собран по БЛОКАМ 1→7; после каждого выполнен чекпоинт:
      `node --check` → строка в NOTES §5 → короткий статус в чате.
- [ ] P7. Шаги не смешаны: болванки создавались пустыми, без догадок о полях и макете.
- [ ] P8. После ШАГА 2 прозвучала ПЕРЕДАЧА: сказано, что макет больше не нужен,
      и предложено вести сборку с новой сессии через `ВОЗОБНОВИ` (RETRO 53).
      Пользователь мог отказаться — тогда достаточно, что предложение было.

## M. Макет и данные

- [ ] M1. HTML не изменён и не перерисован; структура макета воспроизведена в JS.
- [ ] M2. Каждый элемент реестра NOTES §1 покрыт в JS; нет молча потерянного
      функционала.
- [ ] M3. Каждое mock-значение HTML заменено полем SQL либо согласовано с пользователем
      как статический справочник.
- [ ] M4. Ни одного поля и ни одного значения не выдумано агентом (RETRO 12). (auto)
- [ ] M5. Списки SQL output, `CFG.fields` и `FIELDS.md` выписаны рядом и совпадают
      побуквенно (RETRO 17).
- [ ] M6. В `FIELDS.md` указаны тип каждого поля и все ручные настройки Proteus.
- [ ] M7. Читается весь массив `data`, а не `data[0]` (RETRO 11). (auto)
- [ ] M8. Вопросов пользователю было не больше 3, одним сообщением, и каждый
      блокировал сборку. Не было вопросов про дату в snapshot-графике, про `id`
      и ключи вёрстки, про цвета и шрифты из макета (RETRO 22).
- [ ] M9. `CFG.mode` выставлен осознанно: `snapshot` — нет оси времени в макете,
      `timeseries` — есть тренд или переключатель гранулярности.
- [ ] M10. Порядок категорий совпадает с макетом: он выписан в `CFG.order`
      и применён явным `sort`. Сортировка по величине — только там, где сам
      макет отсортирован по величине (RETRO 54). Сверь по census'у
      `scan-layout.py`: он печатает копии в порядке макета.
- [ ] M11. Масштаб повторяющегося блока один на весь вид: ширина/высота
      считаются от максимума ПО ВСЕМУ виду, а не внутри группы. Формула снята
      с ДВУХ копий макета и проверена на ПОСЛЕДНЕЙ — позиция последнего
      элемента совпадает с макетной и умещается в `viewBox` (RETRO 54, 55).

## S. Стоп-баги

- [ ] S1. `option` присваивается глобально, вне функций и IIFE, в БЛОКЕ 7 и в самом
      конце файла; после присваивания `option` больше не мутируется (RETRO 14). (auto)
- [ ] S2. Разметка идёт в отдельный overlay-div, а не напрямую в `[_echarts_instance_]`.
- [ ] S3. Монтаж — самовызываемая IIFE; overlay реально создаётся, а не остаётся
      `null` в невызванной функции (RETRO 1). (auto)
- [ ] S4. Вся разметка пишется только в переменную overlay из замыкания `mount()`.
- [ ] S5. В `buildCSS()` КАЖДЫЙ селектор начинается с `.<ns>-`; голых `div`, `table`,
      `th`, `button`, `*` без префикса нет (RETRO 5). (auto)
- [ ] S6. Каждый CSS-класс из `buildHTML()` имеет правило в `buildCSS()`: стили макета
      перенесены, а не выброшены (RETRO 6). (auto)
- [ ] S7. Тултип в `body` с `position:fixed`, его стили дублированы инлайн (RETRO 7).
- [ ] S8. Скрытие элементов через `style.display`/`style.opacity`, а не `hidden` или
      одного `classList` (RETRO 8). (auto)
- [ ] S9. Обработчики навешиваются ОДИН РАЗ СНАРУЖИ `render()`, делегированием
      от overlay. Внутри тела `render()` нет ни одного `addEventListener` (RETRO 9). (auto)
- [ ] S10. Даты гонятся через `toDate()`: epoch-ms, epoch-s, `YYYY-MM`, `YYYY-MM-DD`
      (RETRO 10). (auto)
- [ ] S11. Нет backticks, template-literals, стрелочных функций, `let`/`const`
      (RETRO 13). (auto)
- [ ] S12. Нет `document.getElementById`; поиск узлов только `overlay.querySelector`.
      Единственное исключение — аварийный `document.querySelector` в `catch` БЛОКА 6,
      где переменной `overlay` может ещё не существовать (RETRO 15). (auto)
- [ ] S13. Корень без собственной карточки: нет двойного фона, рамки, тени, padding.
- [ ] S14. В теле `mount()` есть вызов `render();` вне определения функции: без него
      overlay остаётся пустым, canvas уже скрыт, ошибок в консоли нет (RETRO 25). (auto)
- [ ] S15. В `buildHTML()` префикс БЕЗ точки (`var P = CFG.ns`); точка только в
      `buildCSS()`. В разметке нет `class="."` (RETRO 23). (auto)
- [ ] S16. Позиция fixed-тултипа считается от `getBoundingClientRect()` без вычитания
      rect корня; клампинг по `window.innerWidth/innerHeight`; при нехватке места
      выбирается противоположная сторона (RETRO 26). (auto)
- [ ] S17. Нет `document.addEventListener` и `window.addEventListener` внутри
      `render()`; глобальные слушатели вешаются один раз и защищены флагом
      в `state` (RETRO 2). (auto)
- [ ] S18. Состояние ровно одно — `state` из БЛОКА 2: всё, что читает разметка
      (`buildHTML`/`buildCSS`), присваивается обработчиками именно туда;
      локальных переменных-двойников состояния в `mount()` нет (RETRO 46). (auto)
- [ ] S19. Корень резиновый: рамка демо-страницы макета (фикс-ширина,
      центрирование, фон `body`) не перенесена; у `.<ns>-root` нет фиксированной
      ширины, виджет тянется за ячейкой дашборда (RETRO 48). (auto)
- [ ] S20. Адаптив — от контейнера: оконные `@media (max/min-width)` и `vw`/`vh`
      из макета не скопированы буквально; настоящий брейкпоинт — класс на корне
      по ширине хоста (RETRO 49). (auto)

## C. Каркас и устойчивость

- [ ] C1. В файле ровно 7 БЛОКОВ в порядке шаблона, без перенумерации и пропусков. (auto)
- [ ] C2. Структура и служебный код БЛОКОВ 2, 6, 7 совпадают с
      `templates/TEMPLATE.chart.js`; изменены ТОЛЬКО помеченные `[ЗАПОЛНИ]` места.
- [ ] C3. Canvas скрывается; host становится `position:relative`; старый overlay
      удаляется по `.<ns>-overlay` (RETRO 4). (auto)
- [ ] C4. `ResizeObserver` только правит габариты и НЕ вызывает `render()` (RETRO 3). (auto)
- [ ] C5. Пустой `data` показывает `CFG.text.noData`, и выполнение доходит до БЛОКА 7. (auto)
- [ ] C6. В `catch` ошибка выводится текстом в overlay. Обращение к `option` из `catch`
      ЗАПРЕЩЕНО: на момент монтажа он ещё не присвоен (RETRO 24). (auto)
- [ ] C7. Состояние в `window.__pvtState[CFG.ns]` и восстанавливается (RETRO 2). (auto)
- [ ] C8. Все данные в разметке прогнаны через `esc()`. (auto)

## D. Семантика данных

- [ ] D1. Нет внешних CDN, `import`/`require`, `fetch`, сторонних библиотек. (auto)
- [ ] D2. eCharts не рисует: финальный `option` пустой, со скрытыми осями. (auto)
- [ ] D3. При агрегации явно выбран тип метрики: остаток на конец периода или сумма
      за период; выбор зафиксирован в `FIELDS.md`.
- [ ] D4. Порядок строк задаётся явным `sort` в `buildModel()`; `ORDER BY` из SQL
      не считается гарантией (RETRO 18).
- [ ] D5. Цвета, шрифты, отступы и тексты централизованы в `CFG`, а не размазаны по коду.
- [ ] D6. Все производные величины (доли, проценты, дельты, ранги, накопительные итоги,
      сортировка, форматирование) посчитаны в `buildModel()`, а не в SQL.
      Каждый расчёт, оставленный в SQL, согласован с пользователем и записан
      в NOTES §4 (SKILL.md, правило 2).

## Q и I. Условные секции — живут в своих файлах

Они не дублируются здесь, чтобы этот файл не читался ради неприменимых пунктов.

- **Q1–Q8 (SQL)** — в `SQL.md`, раздел 5. Проходи, только если правил
  `<name>.data.sql`. Не правил — в отчёте одна строка `Q: N/A — SQL не правился`.
- **I1–I16 (интерактив)** — в `TABLES.md`, раздел 6. Проходи, только если читал
  `TABLES.md`. Не читал — одна строка `I: N/A — интерактивной таблицы нет`.

## F. Файлы

- [ ] F1. Есть `<name>.html`, `<name>.data.sql`, `<name>.chart.js`, `<name>.NOTES.md`,
      `FIELDS.md`.
- [ ] F2. В `FIELDS.md` есть точная инструкция по ручным настройкам Proteus.
- [ ] F3. Изменялись только согласованные файлы; существующий код правился точечно.

## Z. Закрытие: доказательство полноты

Делается ПОСЛЕДНИМ. Здесь проверяется не код, а то, что ты ничего не потерял.

- [ ] Z1. M в NOTES §2 равно реальному `wc -l <name>.html`, а диапазоны покрывают
      1..M без дыр. Число M ИЗМЕРЕНО, а не вписано на глаз. (auto)
- [ ] Z2. Каждая строка реестра NOTES §1 имеет статус DONE или STATIC и ссылку
      `<name>.chart.js:строка`. Строк со статусом TODO/ASK не осталось. (auto)
- [ ] Z3. NOTES §5: все семь блоков DONE с отметкой `Syntax OK`. (auto)
- [ ] Z4. NOTES §6 «Открытые хвосты» пуст либо каждый хвост явно проговорён
      пользователю в финальном ответе. (auto)
- [ ] Z6. Отчёт сдан ИМЕННО в формате этого файла: таблица со ВСЕМИ ID из
      пройденных секций. Придумывать свои разделы вместо чек-листа ЗАПРЕЩЕНО —
      так пункты не проверяются, а имитируются (RETRO 43). (auto)
- [ ] Z5. Таблица NOTES §3 «что ждёт макет ↔ что даёт SQL» заполнена, и в колонке
      «Есть в SQL?» не осталось «нет» без строки-решения в §4. Проверь отдельно
      ФОРМУ данных: макет ждёт массив строк или одну агрегатную строку.
      Молчаливая перекройка виджета под имеющиеся колонки = FAIL.
- [ ] Z5b. В NOTES §4 у каждого вопроса заполнена колонка ответа. Прочерк там
      означает «спросил и не дождался»: собранное на таком месте решение —
      виджет не по ТЗ, а в Test6 это прочерки «—» вместо среднего возраста
      прямо в карточках. Не спрашивал — так и напиши словами. Ответа нет и
      ждать нельзя — элемент сдаётся ОТКРЫТЫМ: строка в §6 и абзац в финальном
      ответе, а не молча (RETRO 62). (auto)
- [ ] Z7. В коде нет комментариев вида «не реализовано / для простоты /
      заглушка / пока что»: урезание объёма проговаривается пользователю,
      а не оставляется заметкой на полях (RETRO 61, `validate.py` H4). (auto)

## Формат отчёта

| ID | Статус | Доказательство |
|---|---|---|
| A1 | PASS | вывод скрипта выше |
| P1 | PASS | режим B, сообщение №1 |
| M1 | PASS | `okr.chart.js:120-180` |
| Q1 | N/A | SQL не правился |

Продолжай таблицу по всем ID из пройденных секций.
Обязательное ядро — **71 пункт**: A1–A3 (3), B1–B6 (6), P1–P8 (8), M1–M11 (11),
S1–S20 (20), C1–C8 (8), D1–D6 (6), F1–F3 (3), Z1–Z6 (6).
`validate.py` сверяет твой отчёт с этим списком: ни один ID пропустить нельзя.
Условные, если применимы: Q1–Q8 из `SQL.md` (8), I1–I16 из `TABLES.md` (16).

После таблицы одной строкой:
`Итог: PASS (N/N), где N — число пройденных пунктов; N/A: <список>`
либо список исправлений и повторная таблица.
