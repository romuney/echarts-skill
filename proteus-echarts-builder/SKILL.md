---
name: proteus-echarts-builder
description: "Пишет и правит JS-визуализацию для Proteus (Superset-like BI), где eCharts — пустой холст-контейнер, а вся отрисовка идёт на HTML/CSS/SVG/JS в overlay поверх глобального массива data. Используй, когда просят создать, собрать, доработать или починить интерактивный график, сводную таблицу, кастомную визуализацию или дашборд-виджет в Proteus: дропдауны, drilldown, кроссфильтрация, тултипы, поиск, сортировка, пагинация, экспорт, переключалки метрик и гранулярности. Также используй при правке существующих файлов вида <name>.chart.js и <name>.data.sql, и когда готовый виджет ведёт себя неправильно: не растягивается по ячейке дашборда или застрял в фиксированном размере, тултип не появляется, стили слетели, виджет пустой, цифры не те. Работает в папке проекта: HTML-макет = неизменяемый эталон вёрстки, SQL = источник данных, JS = единственный файл, который создаёт skill."
metadata:
  version: '7.1'
  layer: domain
---

# Proteus eCharts Builder

eCharts НЕ рисует: ему отдаётся пустой `option`, он служит холстом. Визуализация
пишется вручную на HTML+CSS+SVG+JS и монтируется в ОТДЕЛЬНЫЙ overlay-div поверх
хоста. Данные приходят из SQL под графиком как глобальный массив `data`.

Каркас — `templates/TEMPLATE.chart.js`, БЛОКИ 1–7. Макет может быть любым:
не ищи похожий пример и не подгоняй задачу под знакомый тип графика.

# ЧТО ЧИТАТЬ И КОГДА

Контекст — расходный материал: лишний файл сейчас это потерянные строки макета
потом. Читай ТОЛЬКО нужное текущему шагу и ровно один раз.

| Файл | вес | Когда читать |
|---|---|---|
| `SKILL.md` (этот) | 20К | всегда |
| `templates/TEMPLATE.chart.js` | 16К | когда ЗАПОЛНЯЕШЬ блоки. Чтобы скопировать — НЕ читай |
| `PATTERNS.md` | 15К | перед БЛОКОМ 5 и когда что-то «не работает» |
| `RECIPES.md` | 17К | по одному разделу: когда пишешь эту часть или когда отлаживаешь («Сначала мерить, потом править») |
| `SQL.md` | 12К | только если пишешь или правишь `<name>.data.sql` |
| `TABLES.md` | 11К | только если в макете таблица с поиском / сортировкой / пагинацией / экспортом |
| `SELF_CHECK.md` | 17К | перед сдачей |
| `REGRESSION.md` | 4К | перед сдачей, если читал `TABLES.md` |
| `bootstrap.py`, `validate.py`, `smoke.mjs`, `retro-coverage.py` | — | НИКОГДА не читай, только запускай |

**Копирование файла — это `cp`, а не Read+Write.** Прочитать шаблон, чтобы тут же
переписать его без изменений, стоит целого блока контекста и не даёт ничего.
Для болванок есть `bootstrap.py`: один вызов, ноль чтений.

**Не пересказывай прочитанное в ответ.** Файлы читаются, чтобы по ним работать,
а не чтобы цитировать. Пересказ макета в чат — двойной расход контекста.

# ДВА РЕЖИМА РАБОТЫ

Определи режим ПЕРВЫМ действием и назови вслух.

**РЕЖИМ A — новая визуализация** (нет `.chart.js` либо он пустая болванка).
Полный цикл: план → подтверждение → NOTES → блоки 1→7 → проверки → `SELF_CHECK.md`.

**РЕЖИМ B — точечная правка** рабочего `.chart.js` по однозначной задаче
(«подкрась колонку», «почини sticky», «добавь колонку X»). Плана и подтверждения
НЕ жди — сразу минимальный diff. Обязательны: `node --check`, `validate.py`,
`smoke.mjs` и регресс-смоук по затронутым функциям (`REGRESSION.md`, только
затронутые строки). Задача неоднозначна или правка задевает больше двух
функций — это РЕЖИМ A.

# КОНТРАКТ ФАЙЛОВ

| Файл | Роль | Право на правку |
|---|---|---|
| `<name>.html` | Макет-эталон: вёрстка, дизайн, состав элементов | ТОЛЬКО ЧТЕНИЕ |
| `<name>.data.sql` | Источник данных. Каждая колонка = ключ в `data` | Правь ТОЛЬКО с явного «да» |
| `<name>.chart.js` | Единственный результат skill | Создавай блоками |
| `<name>.NOTES.md` | Рабочая память агента. В Proteus НЕ вставляется | Веди постоянно |
| `FIELDS.md` | Сдаточный документ: поля и ручные настройки Proteus | Заполняй в конце |
| `<name>.mock.json` | Пример строк `data` для `smoke.mjs` | По желанию, но с ним проверка точнее |

1. Выходные поля SQL — это и есть массив `data`. Других данных не существует.
2. Mock-данные в макете — заглушки. ЗАМЕНИ каждую на поле из SQL.
3. Данных не хватает → ОСТАНОВИСЬ и СПРОСИ: добавить поле в SQL или оставить
   статическим справочником. НЕ выдумывай колонку и НЕ выкидывай элемент молча.
4. Поле не попадёт в `data`, пока пользователь ВРУЧНУЮ не добавит его
   в «Измерения». Любое предложение добавить или переименовать колонку
   сопровождай этим предупреждением. Имена совпадают ПОБУКВЕННО:
   SQL == `CFG.fields` == `FIELDS.md`.
5. Тип значения ≠ то, что видно в UI. `DATE`/`DATETIME` часто приходят
   epoch-числом — все даты через `toDate()` из БЛОКА 2.

# ЖЕЛЕЗНЫЕ ПРАВИЛА

1. **План до кода — в РЕЖИМЕ A.** Нумерованный план, дождись подтверждения.
   В РЕЖИМЕ B плана нет.

2. **Считает JS, а не ClickHouse.** SQL отдаёт СЫРЫЕ строки нужной гранулярности;
   суммы, доли, проценты, дельты, ранги, накопительные итоги, сортировка
   и форматирование — в `buildModel()`. Перенос расчёта в SQL — только спросив
   и предупредив про «Измерения» (КОНТРАКТ, п. 4). Границы и синтаксис
   ClickHouse 24.8 — в `SQL.md`, читай только когда реально трогаешь SQL.

3. **Память — на диске, а не в контексте.** Первым действием РЕЖИМА A создай
   `<name>.NOTES.md`. Это единственный источник правды: реестр макета, покрытые
   диапазоны, колонки SQL, решения, статус блоков. Пиши ДО следующего действия.
   Держать реестр только в чате ЗАПРЕЩЕНО.

4. **HTML покрывается ПОЛНОСТЬЮ, но не держится в контексте.** Выполни
   `wc -l <name>.html` и запиши число в NOTES §2 как M — `validate.py` (Z1)
   сверяет его с файлом. До 300 строк читай целиком, больше — диапазонами
   по ≤300 строк, и после КАЖДОГО сразу дописывай §1 и §2. Забыл деталь —
   перечитай NOTES, а не HTML. Диапазоны обязаны покрыть 1..M без дыр.
   **Дочитывай до конца, особенно `<script>` макета:** mock-данные и референсная
   реализация в последней трети объявляют, какие поля и какую ФОРМУ данных
   ждёт макет. Выпиши это в §3 и сверь с SQL ДО первой строки кода (RETRO 41).

5. **Код только модульно, с чекпоинтом.** НИКОГДА не генерируй `.chart.js` одним
   куском: по одному БЛОКУ, по порядку 1→7. Блок больше ~250 строк дели на части.
   Чекпоинт после каждого куска: (1) `node --check` — нет `Syntax OK`,
   не продолжай; (2) строка в NOTES §5 `блок N | DONE | Syntax OK | строки a-b`;
   (3) статус в чате ≤3 строк. Без всех трёх следующий блок не начинается.

6. **Возобновление вместо перезапуска.** Контекст обрезан или сессия
   перезапущена — НЕ начинай заново и НЕ перечитывай HTML: прочитай NOTES,
   найди первый пункт со статусом ≠ DONE, продолжай с него.

7. **Каркас копируется, не изобретается.** БЛОКИ 2, 6, 7 — дословно из шаблона,
   меняются только места `[ЗАПОЛНИ]`. Свободны `CFG` (1), `buildModel` (3),
   форматтеры (4) и разметка (5). `getTip/showTip/hideTip/render` — СЛУЖЕБНЫЕ:
   не переписывай, не переименовывай, не пиши рядом свои версии — в них заперты
   инварианты видимости, клампинга и пересборки (RETRO 44). Имена тоже часть
   контракта: по ним ищет `validate.py`. Есть в папке рабочий график — БЛОК 6
   копируется ИЗ НЕГО, меняются только содержимое тултипа и обработчики.

8. **ВОПРОС — КРАЙНЯЯ МЕРА.** Сначала выведи всё, что выводится из макета и SQL.
   Спрашивай только то, без чего график ФИЗИЧЕСКИ не построится: нет числового
   поля под видимый элемент, два равнозначных кандидата, прямое противоречие
   макета и SQL, нужен расчёт в SQL. Одним сообщением, не больше 3 вопросов,
   с вариантами. НИКОГДА не спрашивай: про дату и период, если в макете нет оси
   времени (снимок — норма, ставь `CFG.mode='snapshot'`); про `id`, ключи
   и `data-*`; про цвета, шрифты, отступы и тексты; про «что ещё показать».

9. **Правки точечные.** Рабочий код и макет целиком не переписывай.

10. **`validate.py` обязателен.** Вывод вставляй в ответ КАК ЕСТЬ, пересказывать
    словами нельзя. Хотя бы один FAIL — НЕ СДАНО. В цикле сборки гоняй `--quiet`.
    Но **FAIL — это ГИПОТЕЗА, а не приговор** (RETRO 42): открой место и убедись,
    что баг реальный. Ложное срабатывание чинится в `validate.py`, а не
    переименованиями в чарте.

11. **`smoke.mjs` обязателен.** `validate.py` смотрит на форму кода и не видит
    экрана: график может пройти его целиком и не работать (RETRO 44). Вывод —
    тоже дословно. Коды возврата разные и путать их нельзя: `0` пройдена,
    `1` провалена (НЕ СДАНО), `2` не запускалась (сдача идёт дальше).
    При `2` (нет playwright): проставь `B1`–`B5` = `N/A` с причиной — строки
    с ID должны БЫТЬ, иначе Z6 уронит сдачу; скажи пользователю, что экрана
    никто не видел; предложи `npm i -D playwright && npx playwright install
    chromium`; **сам не ставь** — без явного «да» пакет не ставится.
    Данные берёт из `<name>.mock.json`, иначе генерирует из `CFG.fields`;
    автомок проверяет живость интерфейса, а не правильность цифр.

12. **Не подгоняй код под проверку.** Заглушка «чтобы позеленело» гасит сигнал
    и оставляет баг, а потом выглядит как доказательство исправности
    (RETRO 45). Не проходит — либо чини поведение, либо чини саму проверку
    и скажи вслух. `validate.py` (H2) ловит такие правки и роняет сдачу.

13. **Сдача через `SELF_CHECK.md`.** Таблица PASS/FAIL/N-A с доказательством
    `файл:строка`. Скрипты проверяют каркас и поведение, ты — соответствие
    макету и смысл данных. Есть FAIL — исправь и повтори.

# WORKFLOW РЕЖИМА A

## ШАГ 0. Разведка

`ls` папки ДО любых действий.

- Макета нет → ШАГ 1.
- Макет и/или SQL есть → прочитай SQL целиком, HTML по правилу 4 → ШАГ 2.
- Папки нет → спроси имя/путь → ШАГ 1.
- Есть `<name>.NOTES.md` с незакрытыми пунктами → возобновление (правило 6).

## ШАГ 1. Болванки — одной командой

    python3 bootstrap.py <папка-проекта> [<имя>]

Создаёт папку и пять пустых файлов из `templates/`, сам гоняет `validate.py
--template`. Шаблоны для этого читать НЕ нужно. Ничего не догадывай: не выдумывай
поля в `CFG.fields`, не заполняй `FIELDS.md`, не придумывай макет и SQL.

Затем СТОП: «болванки готовы, пришли HTML-макет и SQL».

## ШАГ 2. Перенос макета в JS

1. Покрой HTML по правилу 4 → NOTES §1 (реестр элементов и mock-значения)
   и §2 (покрытие). В контексте держи NOTES, не HTML.
2. Прочитай SQL → NOTES §3: выходные колонки и типы. Их же в `CFG.fields`.
3. Сопоставь реестр с колонками. Нехватка → вопрос (КОНТРАКТ, п. 3),
   решение → NOTES §4.
4. Перенеси CSS в `buildCSS()`, префиксовав КАЖДЫЙ селектор через `CFG.ns`;
   цвета, шрифты, отступы — в `CFG`. Рамку демо-страницы макета (фон и отступы
   `body`, центрирование, фикс-ширину внешнего контейнера, оконные `@media`)
   НЕ переноси: корень остаётся резиновым (RETRO 48, 49). Как именно — `RECIPES.md`.
5. Перенеси разметку в `buildHTML()` конкатенацией, данные через `esc()`.
6. Напиши `buildModel()` под фактические поля: здесь считается ВСЁ производное.
   Обработчики навесь ОДИН РАЗ СНАРУЖИ `render()`.
7. Заполни `FIELDS.md`: точные имена, типы, чек-лист ручных настроек Proteus.
8. `python3 validate.py <name>.chart.js` — почини все FAIL.
9. `node smoke.mjs <name>.chart.js` — почини все FAIL. Проверки разные: первая
   смотрит на код, вторая на экран. Зелёная первая при красной второй означает
   ровно одно — график не работает.
10. Прогони `SELF_CHECK.md`. Без полного PASS НЕ СДАНО.

Шаги 4–6 идут по правилу 5: блок → чекпоинт → блок.

# СИНТАКСИС И ОКРУЖЕНИЕ JS

Запрещено: backticks и template-literals, стрелочные функции, `let`/`const`,
`document.getElementById`, `console.log` в итоговом файле, внешние библиотеки,
`import`/`require`, `fetch`, чтение DOM вне своего overlay.

Обязательно: `var`, `function`, конкатенация строк, `option` глобально в самом
конце, `try/catch` вокруг монтажа с выводом ошибки текстом в overlay.

**Виджет резиновый.** Хост — ячейка дашборда произвольного размера, живёт рядом
с другими виджетами и меняется при ресайзе. Корень и overlay всегда
`width:100%` от хоста — так в шаблоне, не переопределяй. Макет же — отдельная
страница со своей рамкой (фикс-ширина, центрирование, фон `body`): рамка при
переносе отбрасывается, фикс-размеры остаются только у внутренних элементов
(RETRO 48). Оконные `@media` и `vw`/`vh` в ячейке дашборда не работают: окно —
1920px, ячейка — 400px. Адаптив — от контейнера; рецепт «Рамка макета ≠ рамка
виджета» в `RECIPES.md` (RETRO 49).

Скрытие элементов — только `style.display` или `style.opacity`; `hidden`
и класс без инлайн-стиля ненадёжны. **Спрятал двумя свойствами — снимай оба**
(RETRO 44); для тултипа это делают служебные `showTip/hideTip`.

Состояние только в `window.__pvtState[CFG.ns]`: Proteus перезапускает скрипт на
каждой перерисовке. Глобальные слушатели при перезапуске накапливаются — вешай
их через `state`-флаг, как в шаблоне.

# РУЧНЫЕ НАСТРОЙКИ PROTEUS

Skill не умеет настраивать UI. Измерения, сортировка, упорядочивание, лимит
строк, пагинация и фильтры выставляются руками — выпиши их в `FIELDS.md`.
Если JS опирается на порядок строк, всё равно делай явный `sort` в `buildModel()`.
